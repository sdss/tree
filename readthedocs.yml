version: 2

build:
  os: ubuntu-24.04
  tools:
    python: '3.11'
  jobs:
    pre_install:
      - python bin/copy_data.py

python:
   install:
      - method: pip
        path: .
        extra_requirements:
            - docs

sphinx:
  configuration: docs/sphinx/conf.py
